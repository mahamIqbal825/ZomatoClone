import EStyleSheet from 'react-native-extended-stylesheet';

const styles = EStyleSheet.create({
  container: {
    // flex: 1,
    paddingHorizontal: '-28rem',
    paddingBottom: '15rem'
  },
  customContainer: {
    paddingHorizontal: '28rem'
  },
  recommandedSliderContent: {
    marginBottom: '35rem'
  }
});

export default styles;
