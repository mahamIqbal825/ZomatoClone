import React, { useEffect, useState } from 'react';
import {
    View,
    Text,
    Image,
    TouchableOpacity,
    Share,
    Dimensions,
    FlatList
} from 'react-native';


export function SnackBar({ showMe, }) {

    return (
        <View style={{ position: 'absolute', height: 50, backgroundColor: '#000', bottom: 0, zIndex: 9 }}>

        </View>
    )
}